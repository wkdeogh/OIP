# OIP 구현 계획 명세서

> 최초 작성일: 2026-07-26  
> 상태: 초안 — 참고 이미지, 캘린더 휴무·색상, 공통 비밀번호, 공휴일 권장안 반영 완료

## 1. 프로젝트 개요

### 1.1 앱 이름

**OIP(오입)**

- 5개의 잎을 가진 5잎클로버를 뜻하는 이름
- 부부가 함께 사용하는 생활 관리용 웹 앱

### 1.2 사용자

- 대호
- 상희

초기 버전은 위 두 명만 사용하는 것을 전제로 한다. 다만 기능과 메뉴는 추후 추가하거나 제거하기 쉽도록 구성한다.

### 1.3 목적

부부가 일상에서 함께 확인하고 기록해야 하는 정보를 한 앱에서 관리한다.

- 공동 일정과 개인 일정 관리
- 공동 TODO 관리
- 공동 쇼핑목록 관리
- 여행 계획과 예약 정보 정리
- 냉장고 식품 및 유통기한 관리
- 현재 주차 위치 공유
- 랜덤 여행지, 식사 메뉴 등 생활 편의 기능 제공

### 1.4 기술 스택

- 프론트엔드: Next.js
- 배포: Vercel
- 데이터베이스 및 백엔드: Supabase
- 저장소: <https://github.com/wkdeogh/OIP.git>

세부 기술은 프로젝트 생성 시 아래 구성을 우선 검토한다.

- Next.js App Router
- TypeScript
- Supabase PostgreSQL
- 앱 공통 비밀번호를 이용한 간단 인증
- 모바일 우선 반응형 UI

---

## 2. 핵심 설계 원칙

### 2.1 모바일 우선

실생활에서 휴대폰으로 빠르게 확인하고 입력하는 상황을 우선한다.

- 한 손으로 누르기 쉬운 버튼 크기
- 자주 쓰는 기능은 최소한의 단계로 접근
- 목록과 입력 화면은 작은 화면에서도 읽기 쉽게 구성
- 데스크톱에서도 사용할 수 있도록 반응형 레이아웃 적용

### 2.2 공동 데이터와 개인 데이터 구분

캘린더와 TODO 데이터에는 공개 범위를 둔다.

- `공동`: 대호와 상희 모두 조회 가능
- `개인`: 작성자 본인에게만 표시

각 항목에는 작성자를 저장해 누가 등록했는지 알 수 있게 한다.

쇼핑목록은 두 사람이 함께 확인하는 공동 목록을 기본으로 하고, 각 품목에 추가한 사람과 구매 완료한 사람을 기록한다.

### 2.3 기능 확장성

상위 메뉴는 향후 기능을 추가하거나 제거할 수 있도록 서로 독립적인 모듈로 구현한다.

- 일정
- 여행
- 냉장고
- 주차장
- ETC

### 2.4 단순한 초기 사용자 경험

공통 비밀번호 인증을 마친 뒤 사용자를 선택한다.

- `대호`
- `상희`

선택한 사용자는 기기에 저장하고 다음 접속부터 자동 적용한다. 우측 상단에는 현재 선택된 사용자를 항상 표시하며, 필요할 경우 다른 사용자로 변경할 수 있게 한다.

### 2.5 시각 디자인과 문구

전체 분위기는 **깔끔하면서 약간 아기자기한 귀여운 스타일**로 통일한다.

- 밝은 배경과 채도가 과하지 않은 파스텔 포인트 색상
- 5잎클로버 모티프, 작은 아이콘이나 일러스트를 필요한 곳에만 절제해서 사용
- 장식보다 정보의 가독성과 빠른 조작을 우선
- 그림자와 그라데이션은 강조할 카드나 상태에만 약하게 사용
- 대호, 상희, 공동 항목의 색상은 앱 전체에서 일관되게 사용

문구는 짧고 직접적으로 작성한다.

- 사용: `저장했어요`, `등록된 일정이 없습니다`, `유통기한을 입력해 주세요`
- 사용하지 않음: 과하게 감성적이거나 오글거리는 문구, 기능과 관계없는 응원 문구, 지나친 말장난

---

## 3. 전체 정보 구조 및 화면 이동

### 3.1 최상위 메뉴

앱의 전체 탭 또는 주 내비게이션은 다음 순서로 구성한다.

1. 일정
2. 여행
3. 냉장고
4. 주차장
5. ETC

모바일에서는 하단 탭 바를 우선 검토하고, 화면 너비가 넓을 때는 사이드바 또는 상단 메뉴로 변경할 수 있다.

### 3.2 일정 하위 메뉴

일정 탭 내부에는 다음 서브 탭을 둔다.

- 캘린더
- TODO
- 쇼핑목록

### 3.3 공통 헤더

각 화면 상단에는 다음 요소를 표시한다.

- 현재 화면 제목
- 현재 사용자: `대호` 또는 `상희`
- 사용자 변경 메뉴

필요한 화면에서는 우측 하단의 고정 추가 버튼 또는 헤더의 추가 버튼을 제공한다.

---

## 4. 최초 진입, 공통 비밀번호 및 사용자 선택

### 4.1 공통 비밀번호 화면

인증되지 않은 기기에서 앱에 처음 접속하면 앱 본문보다 먼저 공통 비밀번호 입력 화면을 표시한다.

```text
OIP

비밀번호
[                ]
[ 확인 ]
```

- 대호와 상희가 같은 앱 비밀번호를 사용
- 비밀번호가 맞으면 해당 기기를 인증된 기기로 등록
- 틀린 경우 입력창 가까이에 오류 표시
- 여러 번 연속 실패하면 짧은 시간 동안 재시도를 제한
- 비밀번호 확인 전에는 Supabase 생활 데이터에 접근할 수 없게 함

### 4.2 인증 상태 기기 저장

- 원문 비밀번호를 `localStorage`에 저장하지 않음
- 서버에서 비밀번호 검증 후 서명된 장기 세션을 발급
- 인증 상태는 `HttpOnly`, `Secure`, `SameSite` 쿠키로 기기에 저장
- 세션 유효기간은 초기안으로 90일을 사용하고, 접속 시 안전하게 갱신
- 설정에서 `이 기기 인증 해제`를 제공
- 앱 공통 비밀번호를 변경하면 기존 기기 세션을 모두 무효화할 수 있게 세션 버전을 관리
- 비밀번호 해시와 세션 서명 키는 Vercel 환경 변수에 저장하며 클라이언트 코드에 노출하지 않음

### 4.3 사용자 선택

공통 비밀번호 인증 후 기기에 저장된 사용자 정보가 없으면 사용자를 선택한다.

```text
사용자를 선택해 주세요.

[ 대호 ]  [ 상희 ]
```

- 선택값은 `localStorage`에 저장
- 저장 키 예시: `oip.currentUser`
- 값 예시: `daeho`, `sanghee`
- 다음 접속부터 마지막으로 선택한 사용자 자동 적용

### 4.4 사용자 변경

- 공통 헤더 우측 상단에서 현재 사용자를 확인
- 사용자 이름을 누르면 대호와 상희 중 변경 가능
- 사용자 변경 시 개인 일정과 개인 TODO 목록을 새 사용자 기준으로 다시 조회

### 4.5 보안 범위

공통 비밀번호는 외부인의 앱 접근을 막기 위한 간단한 인증이다. 대호와 상희는 같은 비밀번호를 사용하고 앱 안에서 사용자를 자유롭게 바꿀 수 있으므로, 개인 일정은 상대방에 대한 보안 비밀이 아니라 각자 화면을 정리하기 위한 표시 범위로 본다.

데이터 접근은 인증 쿠키를 검증하는 Next.js 서버 경로 또는 Server Action을 통해 처리하고, 브라우저에서 Supabase 서비스 역할 키를 직접 사용하지 않는다.

---

## 5. 일정 기능

### 5.1 캘린더

### 목적

부부의 공동 일정과 각자의 개인 일정을 월 단위로 확인하고 관리한다.

### 기본 화면

- UI 참고 이미지: [`calander_example.jpeg`](./calander_example.jpeg)
- 월간 캘린더를 기본 화면으로 표시
- 상단에서 현재 연월을 크게 표시하고 이전 달, 다음 달 이동 및 연월 선택 기능 제공
- 일요일은 붉은 계열로 구분하고 이전·다음 달의 날짜는 흐리게 표시
- 오늘 또는 선택한 날짜는 원형 배경으로 강조
- 월간 그리드 안에 일정을 짧은 색상 칩으로 직접 표시
- 여러 날짜에 걸친 일정은 날짜 칸을 가로지르는 연속 막대로 표시
- 일정 칩에는 화면 너비가 허용하는 범위에서 시간과 제목을 표시
- 한 날짜에 표시 공간보다 일정이 많으면 `+N`으로 접고, 날짜 선택 시 전체 목록 표시
- 날짜를 선택하면 해당 날짜의 일정 목록과 일정 추가 동작을 제공
- 월을 이동할 때 방향에 맞는 짧은 슬라이드 전환 적용

참고 이미지의 핵심인 `한눈에 많은 일정을 확인하는 월간 밀도`와 `색상 일정 칩`은 유지한다. 광고, 알림 아이콘, 원본 앱 전용 하단 메뉴처럼 OIP에 필요하지 않은 요소는 그대로 복제하지 않는다.

### 기본 등록 일정

다음 기념일은 매년 반복되는 기본 일정으로 제공한다.

| 날짜 | 일정 |
|---|---|
| 4월 15일 | 대호 생일 |
| 5월 6일 | 상희 생일 |
| 8월 30일 | 결혼기념일 |
| 11월 17일 | 만난 날 기념일 |

대한민국 공휴일도 기본 표시한다.

- 기본 기념일과 공휴일은 일반 사용자 일정과 시각적으로 구분
- 공휴일은 기본적으로 수정 또는 삭제 불가
- 기념일의 수정 및 숨김 허용 여부는 추후 결정

#### 대한민국 공휴일 데이터 권장안

공휴일은 [공공데이터포털의 한국천문연구원 특일 정보 API](https://www.data.go.kr/data/15012690/openapi.do)를 공식 원본으로 사용한다. 이 API는 국경일, 공휴일, 기념일과 공공기관 휴일 여부를 제공한다.

앱 화면에서 외부 API를 매번 직접 호출하지 않고 다음 방식으로 운영한다.

1. 서버 작업이 현재 연도와 다음 연도의 공휴일을 API에서 조회
2. `public_holidays` 테이블에 날짜와 명칭을 Upsert
3. 캘린더 화면은 Supabase에 저장된 공휴일을 조회
4. 정기 동기화에 실패하면 마지막으로 정상 저장된 데이터를 계속 사용
5. 임시공휴일과 선거일 반영을 위해 현재 연도 데이터는 월 1회 갱신하고, 관리자용 수동 동기화 기능도 제공

API 인증키는 서버 환경 변수에 저장하고 브라우저에는 노출하지 않는다. 초기 배포 시 현재 연도와 다음 연도의 데이터를 미리 동기화해 API 장애가 캘린더 조회에 영향을 주지 않게 한다.

### 일정 추가

일정 추가 시 다음 정보를 입력한다.

- 제목: 필수
- 날짜: 필수
- 시작 시간: 선택
- 종료 시간: 선택
- 종일 여부
- 메모: 선택
- `공동 일정` 체크박스
- 작성자: 현재 선택된 사용자로 자동 기록
- 색상: 기본색 또는 사용자 지정색

`공동 일정`은 기본적으로 체크하지 않는다. 체크하지 않으면 현재 사용자의 개인 일정으로 등록되고, 체크하면 두 사용자에게 표시되는 공동 일정으로 등록된다.

### 일정 색상 규칙

일정 칩에는 다음 기본 색상 토큰을 사용한다. 정확한 색상값은 전체 디자인 시안에서 확정한다.

- 상희 개인 일정: 상희 기본색
- 대호 개인 일정: 대호 기본색
- 공동 일정: 공동 기본색
- 공휴일 및 시스템 일정: 시스템 기본색

일정 추가 화면의 색상 선택기는 위 규칙에 맞는 기본색을 먼저 선택한 상태로 연다.

- 현재 사용자가 대호이고 공동 일정이 아니면 대호 기본색
- 현재 사용자가 상희이고 공동 일정이 아니면 상희 기본색
- `공동 일정`을 체크하면 공동 기본색으로 자동 변경
- 사용자가 직접 다른 색을 선택했다면 해당 일정에는 사용자 지정색을 우선 적용
- 공동 체크를 켜거나 끌 때 사용자가 이미 직접 선택한 색은 임의로 덮어쓰지 않음
- 색상 선택은 전체 색상 입력보다 가독성이 검증된 제한된 파스텔 팔레트를 우선 제공
- 사용자 지정색을 사용해도 작성자 이름과 공동 일정 표시는 별도 라벨 또는 아이콘으로 유지

### 휴무 추가

일반 일정과 별도로 `휴무 추가` 기능을 제공한다. 캘린더의 빠른 추가 메뉴는 다음 두 동작으로 나눈다.

- `일정 추가`
- `휴무 추가`

휴무 추가 항목:

- 날짜: 필수
- 사용자: 현재 사용자로 자동 선택, 필요하면 대호 또는 상희로 변경
- 휴무 종류: 필수
- 메모: 선택

사용자별 선택 가능한 기본 휴무 종류:

- 공통: 연차, 반차, 기타 휴무
- 대호: 패밀리데이
- 상희: 해피프라이데이

사용자를 바꾸면 해당 사용자에게 맞지 않는 전용 휴무 종류는 선택 목록에서 제외한다.

### 휴무 표시 규칙

- 휴무는 일정 칩이 아니라 해당 날짜 칸의 **연한 배경색**으로 표시
- 상희 휴무는 상희 휴무 기본색, 대호 휴무는 대호 휴무 기본색 사용
- 휴무 종류는 날짜 상세 목록에 `연차`, `패밀리데이`, `해피프라이데이`처럼 텍스트로 표시
- 같은 날짜에 대호와 상희가 모두 연차이면 날짜 배경을 두 사람의 휴무색이 절반씩 보이는 2색 그라데이션으로 표시
- 같은 날짜에 두 사람의 다른 휴무가 겹치는 경우에도 동일한 2색 배경을 사용하고, 상세 목록에서 각 휴무 종류를 구분
- 배경색 위에 일정 칩과 날짜 숫자가 충분한 명암비로 보이도록 색 농도를 낮게 유지
- 색상만으로 상태를 판단하지 않도록 날짜 상세에 사용자 이름과 휴무 종류를 함께 표시
- 반차는 `오전` 또는 `오후`를 추가로 선택할 수 있게 하고 상세 목록에서 구분
- 휴무 기록은 두 사용자 모두 조회 가능

### 일정 표시 규칙

- 공동 일정은 두 사용자 모두에게 표시
- 개인 일정은 작성자 본인에게만 표시
- 일정 목록에 작성자 이름과 작성자별 기본 색상 적용
- 기본 시스템 일정은 별도의 아이콘 또는 색상 사용
- 날짜가 같은 일정은 종일 일정과 시간 지정 일정을 구분해 정렬
- 색상만으로 작성자와 일정 종류를 구분하지 않고 텍스트, 아이콘 또는 모양을 함께 사용

### 일정 수정 및 삭제

- 사용자가 등록한 일정은 수정 및 삭제 가능
- 삭제 전 확인창 표시
- 다른 사람이 작성한 공동 일정을 수정 또는 삭제할 수 있게 할지는 추후 결정
- 초기 버전에서는 두 사용자 모두 공동 일정을 수정할 수 있도록 구현하는 것을 기본안으로 한다.

### 5.2 TODO

### 목적

부부가 해야 할 일을 공동 또는 개인 단위로 기록하고 완료 여부를 관리한다.

### TODO 목록

- 미완료 항목을 먼저 표시
- 완료 항목은 아래에 분리하거나 접을 수 있게 표시
- 공동 TODO와 개인 TODO를 구분
- 작성자를 이름 또는 색상으로 표시
- 마감일이 있으면 가까운 순서대로 정렬

### TODO 추가

초기 버전의 입력 항목은 다음과 같다.

- 할 일 제목: 필수
- 상세 메모: 선택
- 마감일: 선택
- 공개 범위: `공동` 또는 `개인`
- 작성자: 현재 사용자로 자동 기록

### TODO 동작

- 완료 및 미완료 전환
- 내용 수정
- 항목 삭제
- 완료 시간 기록
- 개인 TODO는 작성자 본인에게만 표시

### 추후 검토 기능

- 담당자 지정
- 우선순위
- 반복 TODO
- 캘린더 일정과 연결
- 드래그를 이용한 순서 변경

### 5.3 쇼핑목록

### 목적

대호와 상희가 함께 사야 할 물건을 빠르게 추가하고, 장을 보면서 구매 여부를 확인한다.

### 기본 화면

- 화면 상단에 품목명 빠른 입력창과 `추가` 버튼 배치
- 미구매 품목을 먼저 표시
- 구매 완료 품목은 아래의 완료 영역으로 이동
- 각 품목에 추가한 사용자 표시
- 구매 완료한 품목에는 완료한 사용자와 완료 시각 기록
- 두 사용자가 같은 공동 목록을 조회

### 품목 추가

입력 항목:

- 품목명: 필수
- 수량: 선택, 기본값 1
- 단위: 선택
- 분류: 식품 / 생활용품 / 의약품 / 기타
- 메모: 선택
- 추가한 사용자: 현재 사용자로 자동 기록

품목명만 입력해도 즉시 추가할 수 있게 하고, 수량이나 메모 같은 세부 정보는 필요할 때 펼쳐 입력한다.

### 쇼핑 중 동작

- 품목 행 전체 또는 충분히 큰 체크 버튼으로 구매 완료 및 미완료 전환
- 체크 즉시 완료 상태를 화면에 반영하고 저장 실패 시 원상 복구
- 품목 수정 및 한 건 삭제
- 구매 완료 품목 전체 삭제
- 삭제 전에 확인 또는 `실행 취소` 제공
- 두 사용자가 동시에 보고 있을 때 변경 내용을 빠르게 동기화

### 정렬 규칙

1. 미구매 품목
2. 구매 완료 품목

미구매 품목은 최근 추가순을 기본으로 한다. 필요하면 카테고리별 묶기 또는 직접 순서 정렬을 추후 추가한다.

### 추후 검토 기능

- 냉장고 아이템을 쇼핑목록으로 다시 추가
- 구매 완료 품목을 냉장고 아이템으로 전환
- 자주 사는 품목 빠른 추가
- 카테고리 필터
- 쇼핑 이력

---

## 6. 여행 기능

### 6.1 여행 목록

여행 탭에서는 등록된 여행 목록과 `여행 추가` 버튼을 표시한다.

여행 카드에 표시할 기본 정보:

- 여행 제목
- 여행지
- 여행 시작일 및 종료일
- 대표 이미지: 선택 기능으로 추후 추가 가능

여행 목록은 두 영역으로 구분한다.

#### 예정 및 진행 중인 여행

- 여행 종료일이 오늘 이후이거나 오늘인 여행
- 출발일이 오늘과 가까운 순서, 즉 `start_date ASC`로 정렬
- 이미 출발했지만 아직 종료되지 않은 여행은 목록 최상단에서 `여행 중` 상태로 표시

#### 지난 여행

- 여행 종료일이 오늘보다 이전인 여행
- 예정 및 진행 중인 여행 아래의 `지난 여행` 영역에 별도로 표시
- 가장 최근에 끝난 여행부터 확인할 수 있도록 `end_date DESC`로 정렬
- 지난 여행 목록은 필요하면 접거나 펼칠 수 있게 구성

### 6.2 여행 추가 및 기본 정보

여행 생성 시 다음 정보를 입력한다.

- 여행 제목: 필수
- 여행지: 필수
- 출발일: 필수
- 종료일: 필수
- 전체 메모: 선택
- 작성자: 현재 사용자로 자동 기록

여행 상세 화면에서는 정보를 섹션별로 나누어 관리한다.

### 6.3 비행기 정보

하나의 여행에 비행편을 여러 개 추가할 수 있어야 한다. 경유편도 기록할 수 있도록 단일 `가는 편/오는 편` 필드로 제한하지 않고 목록 구조로 저장한다.

입력 항목:

- 구분: 가는 편 / 오는 편 / 기타
- 출발지
- 출발 공항
- 출발 날짜
- 출발 시간
- 도착지
- 도착 공항
- 도착 날짜
- 도착 시간
- 항공사
- 편명
- 예약번호: 선택
- 좌석 정보: 선택
- 수하물 정보
- 가격: 선택
- 비고

### 6.4 숙소 정보

하나의 여행에 숙소를 여러 개 추가할 수 있어야 한다.

입력 항목:

- 숙소 이름
- 위치 또는 주소
- 지도 링크: 선택
- 체크인 날짜
- 체크인 시간
- 체크아웃 날짜
- 체크아웃 시간
- 예약번호: 선택
- 가격
- 연락처: 선택
- 비고

### 6.5 교통 정보

비행기 외에 여행지에서 이용할 교통수단을 기록한다.

입력 항목:

- 교통수단 종류: 기차 / 버스 / 렌터카 / 택시 / 선박 / 기타
- 제목
- 출발 위치
- 출발 날짜 및 시간
- 도착 위치
- 도착 날짜 및 시간
- 예약 정보
- 가격
- 링크
- 비고

### 6.6 먹고 싶은 것

여행 중 먹고 싶은 메뉴나 방문할 식당을 목록으로 관리한다.

입력 항목:

- 이름
- 종류: 음식 / 식당
- 위치
- 지도 또는 참고 링크
- 가격대: 선택
- 방문 완료 여부
- 메모

### 6.7 가고 싶은 곳

관광지, 쇼핑 장소 등 방문 후보를 목록으로 관리한다.

입력 항목:

- 장소 이름
- 분류: 관광 / 쇼핑 / 체험 / 기타
- 위치
- 지도 또는 참고 링크
- 방문 희망일: 선택
- 방문 완료 여부
- 메모

### 6.8 여행 상세 화면 구성안

```text
[여행 제목 / 여행 기간]

[개요] [비행기] [숙소] [교통] [먹을 것] [갈 곳]
```

화면이 복잡해지지 않도록 모바일에서는 섹션 탭 또는 접이식 카드 UI를 사용한다.

### 6.9 추후 검토 기능

- 날짜별 여행 일정표
- 여행 경비 합계
- 지도 연동
- 예약 문서 및 이미지 첨부
- 여행 완료 후 사진 및 후기 기록
- 링크 미리보기

---

## 7. 냉장고 기능

### 7.1 기본 화면

- 상단 또는 우측 하단에 `아이템 추가` 버튼 표시
- 등록된 모든 아이템을 목록으로 표시
- 유통기한이 적게 남은 순서대로 정렬
- 이미 지난 유통기한은 목록 최상단에서 경고 색상으로 표시

### 7.2 아이템 추가

입력 항목:

- 아이템 이름: 필수
- 수량: 선택, 기본값 1
- 단위: 선택
- 유통기한: 필수
- 보관 위치: 냉장 / 냉동 / 실온 / 기타
- 카테고리: 선택
- 구입일: 선택
- 메모: 선택
- 등록자: 현재 사용자로 자동 기록

### 7.3 목록 표시

각 아이템에는 다음 정보를 우선 표시한다.

- 이름
- 수량
- 보관 위치
- 유통기한
- 남은 기간: 예) `D-3`, `오늘까지`, `2일 지남`

정렬 규칙:

1. 유통기한이 지난 아이템
2. 유통기한이 가까운 아이템

같은 날짜인 경우 최근 등록한 아이템을 먼저 표시한다.

### 7.4 아이템 동작

- 내용 수정
- 삭제
- 소비 완료 처리
- 수량 빠른 변경

소비 완료된 아이템을 즉시 삭제할지, 이력으로 보관할지는 추후 결정한다. 초기 버전에서는 완료 시간을 기록한 뒤 기본 목록에서 숨기는 방식을 우선한다.

### 7.5 추후 검토 기능

- 바코드 스캔
- 자주 구매하는 아이템 빠른 추가
- 장보기 목록 연동
- 냉장고와 냉동실 필터

---

## 8. 주차장 기능

### 8.1 목적

마지막으로 등록한 차량의 현재 주차 위치를 두 사용자가 빠르게 확인하고 업데이트한다.

### 8.2 주차 가능 위치

- 지하층: B4, B5, B6
- 기둥 문자: A, B, C, D
- 기둥 번호: 1, 2, 3, 4

### 8.3 기본 화면

- UI 참고 이미지: [`parking_example.jpeg`](./parking_example.jpeg)

화면 최상단에 현재 주차 위치를 크게 표시한다.

```text
현재 주차 위치
B4 D4

마지막 수정: 7월 26일 오후 8:30
수정한 사람: 대호
```

그 아래에 위치를 업데이트하는 UI를 배치한다.

- 층 선택: B4 / B5 / B6
- 기둥 문자 선택: A / B / C / D
- 기둥 번호 선택: 1 / 2 / 3 / 4
- 선택 결과가 반영된 `B5 C4 저장하기` 형태의 버튼

참고 이미지에서 다음 구성을 반영한다.

- 현재 위치를 별도 카드에서 가장 먼저 크게 표시
- 현재 위치 카드에 수정한 사용자와 수정 시각을 함께 표시
- `층 → 기둥 문자 → 기둥 번호` 순서의 큰 선택 버튼 그리드
- 선택한 버튼은 채워진 배경색과 테두리로 명확히 강조
- 둥근 카드, 부드러운 그림자, 따뜻한 크림·오렌지 계열을 OIP 전체 디자인과 조화되게 사용
- 저장 버튼의 문구를 현재 선택값에 따라 즉시 변경
- 참고 이미지의 별도 앱 이름, 접속 상태, 이름 변경 영역은 OIP 공통 헤더와 중복되므로 제외

### 8.4 업데이트 동작

- 현재 위치는 가장 최근에 등록한 한 건을 대표값으로 표시
- 업데이트한 사용자와 시간을 함께 저장
- 잘못 입력한 경우 바로 다시 수정 가능
- 네트워크 저장 중에는 중복 제출 방지
- 저장 성공 및 실패 상태를 사용자에게 명확히 안내
- 모든 세 가지 값이 선택되기 전에는 저장 버튼 비활성화
- 저장 직후 현재 위치 카드를 새 위치로 즉시 갱신하고, 실패하면 이전 값으로 복구

### 8.5 주차 이력

초기 화면에는 현재 위치만 강조한다. 데이터베이스에는 변경 이력을 남겨 최근 기록을 확인할 수 있도록 설계한다.

추후 필요하면 다음 기능을 추가한다.

- 최근 주차 위치 목록
- 이전 위치로 되돌리기
- 주차 사진 첨부
- 주차 위치 메모

---

## 9. ETC 기능

ETC는 소규모 생활 편의 기능을 모아두는 메뉴로 사용한다. 기능이 늘어나면 독립된 상위 메뉴로 분리할 수 있다.

### 9.1 랜덤 여행지 선택

사용자가 후보 여행지를 직접 등록하고 그중 하나를 무작위로 선택한다.

기본 동작:

- 여행지 후보 추가
- 후보 수정 및 삭제
- 후보 활성화 및 비활성화
- `랜덤 선택` 버튼
- 선택 과정에 간단한 애니메이션 제공
- 선택 결과 다시 뽑기

추후 검토:

- 국내 / 해외 필터
- 예산, 계절, 이동시간 조건
- 최근 선택된 여행지 제외
- 여행 기능에서 등록된 여행지와 연결

### 9.2 랜덤 식사 메뉴 선택

사용자가 후보 메뉴를 직접 등록하고 그중 하나를 무작위로 선택한다.

기본 동작:

- 메뉴 후보 추가
- 후보 수정 및 삭제
- 분류: 한식 / 중식 / 일식 / 양식 / 배달 / 외식 / 기타
- 분류 필터 후 랜덤 선택
- 결과 다시 뽑기

추후 검토:

- 최근 먹은 메뉴 제외
- 배달 또는 외식 조건
- 선호도 가중치
- 식당 후보까지 함께 선택

---

## 10. 데이터 모델 초안

실제 테이블명과 컬럼은 구현 중 변경될 수 있다.

### 10.1 사용자 `profiles`

| 컬럼 | 설명 |
|---|---|
| `id` | 사용자 ID |
| `code` | `daeho` 또는 `sanghee` |
| `display_name` | 대호 또는 상희 |
| `event_color` | 개인 일정 기본색 |
| `day_off_color` | 휴무 배경 기본색 |
| `created_at` | 생성 시각 |

### 10.2 캘린더 일정 `calendar_events`

| 컬럼 | 설명 |
|---|---|
| `id` | 일정 ID |
| `title` | 제목 |
| `start_at` | 시작 일시 |
| `end_at` | 종료 일시 |
| `is_all_day` | 종일 여부 |
| `memo` | 메모 |
| `visibility` | `shared` 또는 `private` |
| `author_id` | 작성자 |
| `event_type` | 일반 / 기념일 |
| `color_mode` | `default` 또는 `custom` |
| `custom_color` | 사용자 지정색, 기본색 사용 시 `null` |
| `recurrence_rule` | 반복 규칙 |
| `created_at`, `updated_at` | 생성 및 수정 시각 |

공동 일정은 `visibility = shared`로 저장하며 화면에서는 공동 기본색을 사용한다. `custom_color`가 있으면 기본색보다 우선하지만, 작성자와 공동 여부는 색상 외의 정보로도 계속 표시한다.

#### 10.2.1 휴무 `calendar_days_off`

| 컬럼 | 설명 |
|---|---|
| `id` | 휴무 ID |
| `date` | 휴무 날짜 |
| `owner_id` | 대호 또는 상희 |
| `day_off_type` | 연차 / 반차 / 패밀리데이 / 해피프라이데이 / 기타 |
| `half_day_period` | 반차일 때 오전 / 오후 |
| `memo` | 메모 |
| `created_at`, `updated_at` | 생성 및 수정 시각 |

한 날짜에 두 사용자의 휴무가 각각 존재할 수 있도록 `(date, owner_id)` 기준 조회를 지원한다. 두 사용자 휴무가 겹치면 화면에서 두 휴무색을 합성하고 원본 데이터는 별도 행으로 유지한다.

#### 10.2.2 대한민국 공휴일 `public_holidays`

| 컬럼 | 설명 |
|---|---|
| `date` | 공휴일 날짜 |
| `name` | 공휴일 명칭 |
| `is_holiday` | 공공기관 휴일 여부 |
| `source` | 데이터 원본 |
| `synced_at` | 마지막 동기화 시각 |

한국천문연구원 특일 정보 API의 결과를 날짜 기준으로 Upsert한다.

### 10.3 TODO `todos`

| 컬럼 | 설명 |
|---|---|
| `id` | TODO ID |
| `title` | 제목 |
| `memo` | 상세 메모 |
| `due_at` | 마감 일시 |
| `is_completed` | 완료 여부 |
| `completed_at` | 완료 시각 |
| `visibility` | `shared` 또는 `private` |
| `author_id` | 작성자 |
| `created_at`, `updated_at` | 생성 및 수정 시각 |

### 10.4 쇼핑목록 `shopping_items`

| 컬럼 | 설명 |
|---|---|
| `id` | 품목 ID |
| `name` | 품목명 |
| `quantity`, `unit` | 수량 및 단위 |
| `category` | 품목 분류 |
| `memo` | 메모 |
| `is_purchased` | 구매 완료 여부 |
| `added_by` | 추가한 사용자 |
| `purchased_by` | 구매 완료한 사용자 |
| `purchased_at` | 구매 완료 시각 |
| `created_at`, `updated_at` | 생성 및 수정 시각 |

쇼핑 품목은 기본적으로 공동 데이터이며 대호와 상희 모두 조회하고 변경할 수 있다.

### 10.5 여행 `trips`

| 컬럼 | 설명 |
|---|---|
| `id` | 여행 ID |
| `title` | 여행 제목 |
| `destination` | 여행지 |
| `start_date`, `end_date` | 여행 기간 |
| `memo` | 전체 메모 |
| `author_id` | 작성자 |
| `created_at`, `updated_at` | 생성 및 수정 시각 |

여행 하위 정보는 각각 별도 테이블로 관리한다.

- `trip_flights`
- `trip_accommodations`
- `trip_transportations`
- `trip_foods`
- `trip_places`

각 하위 테이블은 `trip_id`로 여행과 연결하고, 한 여행에 여러 항목을 등록할 수 있게 한다.

### 10.6 냉장고 `fridge_items`

| 컬럼 | 설명 |
|---|---|
| `id` | 아이템 ID |
| `name` | 이름 |
| `quantity`, `unit` | 수량 및 단위 |
| `expiration_date` | 유통기한 |
| `storage_type` | 냉장 / 냉동 / 실온 / 기타 |
| `category` | 카테고리 |
| `purchased_at` | 구입일 |
| `memo` | 메모 |
| `author_id` | 등록자 |
| `consumed_at` | 소비 완료 시각 |
| `created_at`, `updated_at` | 생성 및 수정 시각 |

### 10.7 주차 이력 `parking_records`

| 컬럼 | 설명 |
|---|---|
| `id` | 기록 ID |
| `floor` | B4 / B5 / B6 |
| `pillar_letter` | A / B / C / D |
| `pillar_number` | 기둥 번호 |
| `author_id` | 수정한 사용자 |
| `memo` | 선택 메모 |
| `created_at` | 등록 시각 |

가장 최신 `created_at`의 기록을 현재 주차 위치로 사용한다.

### 10.8 랜덤 후보 `random_candidates`

| 컬럼 | 설명 |
|---|---|
| `id` | 후보 ID |
| `type` | 여행지 / 식사 메뉴 |
| `name` | 후보 이름 |
| `category` | 분류 |
| `is_active` | 랜덤 선택 포함 여부 |
| `memo` | 메모 |
| `author_id` | 등록자 |
| `created_at`, `updated_at` | 생성 및 수정 시각 |

---

## 11. 데이터 접근 및 보안

### 11.1 공통 원칙

- 모든 입력값은 프론트엔드와 서버 양쪽에서 검증
- 모든 생활 데이터 요청에서 공통 비밀번호 인증 세션을 서버에서 검증
- Supabase 환경 변수는 공개용 키와 서버 전용 키를 구분
- 서비스 역할 키는 브라우저 코드에 포함하지 않음
- 브라우저가 Supabase 테이블을 서비스 역할 권한으로 직접 읽거나 수정하지 않음
- 데이터 변경 실패 시 화면에 오류 안내
- 생성 및 수정 시간을 모든 주요 데이터에 기록

### 11.2 개인 데이터 처리

개인 일정과 개인 TODO는 조회 단계에서 현재 사용자 기준으로 필터링한다.

공통 비밀번호는 외부 접근을 막지만 대호와 상희를 서로 다른 계정으로 인증하지는 않는다. 사용자 선택값은 편의를 위한 화면 필터이며 앱 안에서 변경할 수 있다. 따라서 개인 항목은 상대방에게 숨겨진 화면 데이터로 취급하되, 두 사람 사이의 보안 비밀을 저장하는 용도로 사용하지 않는다.

### 11.3 실시간 반영

한 사람이 수정한 내용을 다른 사람의 화면에 자동 반영할지는 1차 버전에서 선택한다.

- 기본안: 화면 진입 및 저장 후 재조회
- 개선안: Supabase Realtime으로 일정, TODO, 쇼핑목록, 주차 위치 등을 즉시 동기화

쇼핑목록과 주차 위치는 두 사람이 동시에 확인하거나 수정할 가능성이 있으므로 Realtime 적용 우선순위를 높게 둔다.

---

## 12. UX 상호작용, 상태 및 오류 처리

모든 주요 화면에서 다음 상태를 제공한다.

- 로딩 중
- 데이터 없음
- 조회 실패
- 저장 중
- 저장 성공
- 저장 실패

삭제, 완료 처리, 주차 위치 변경처럼 결과가 즉시 보여야 하는 동작은 낙관적 업데이트 사용 여부를 기능별로 결정한다. 오류 발생 시 이전 상태로 되돌릴 수 있어야 한다.

### 12.1 필수 구현

다음 항목은 모든 관련 화면에서 공통 품질 기준으로 적용한다.

#### 터치 및 클릭 피드백

- 모바일 터치를 우선 기준으로 모든 버튼, 탭, 체크 항목과 카드형 링크에 누름 상태 제공
- 아이콘 버튼과 보조 조작은 최소 `44×44px`, 주요 버튼은 최소 높이 `48px` 확보
- 손가락이 닿는 즉시 색상·명도 변화와 `scale(0.97~0.99)` 수준의 미세한 축소를 적용
- 누름 피드백은 서버 응답을 기다리지 않고 즉시 시작하며, 체감 지연이 없도록 pressed 상태 전환은 약 `100ms` 이내로 구성
- 손가락을 떼거나 취소하면 자연스럽게 원래 상태로 복귀
- 저장 중인 버튼은 로딩 상태와 비활성 상태를 표시해 중복 입력 방지
- disabled 상태에서는 눌리는 애니메이션을 표시하지 않음
- 쇼핑 품목, TODO, 냉장고 항목처럼 자주 누르는 행은 체크 영역을 작게 한정하지 않고 행 전체에 넓은 터치 영역 제공
- 스크롤 중 잘못 눌리는 것을 줄이도록 터치 시작과 실제 클릭을 구분하고, 수평 제스처에만 의존하는 핵심 동작은 만들지 않음
- `touch-action: manipulation` 등 모바일 브라우저에 적합한 설정을 사용하되 확대와 접근성을 방해하지 않음
- 색상 변화만으로 누름 여부를 표현하지 않고 크기, 명도 또는 테두리 변화를 함께 사용

#### Hover 및 Focus 상태

- 마우스 사용자를 위한 `hover` 상태 제공
- 키보드 사용자를 위한 명확한 `focus-visible` 테두리 제공
- 터치 기기에서는 hover에 의존하는 정보나 동작을 만들지 않음
- 브라우저 기본 포커스를 이유 없이 제거하지 않음

#### Skeleton Loading

- 최초 데이터 조회 시 실제 화면 구조와 비슷한 스켈레톤 표시
- 캘린더, TODO, 쇼핑목록, 여행 카드, 냉장고 목록, 현재 주차 위치 카드에 각각 전용 형태 사용
- 짧은 저장 동작에는 전체 스켈레톤 대신 버튼 로딩 또는 부분 로딩 사용
- 스켈레톤이 불필요하게 깜빡이지 않도록 아주 빠른 응답에서는 표시 지연을 검토

#### Toast / Snackbar

- 저장, 수정, 삭제, 복구 완료처럼 화면 흐름을 막을 필요가 없는 결과에 사용
- 실패 메시지에는 원인과 가능한 다음 행동을 간단히 표시
- 삭제 직후에는 가능하면 `실행 취소` 제공
- 폼 입력 오류는 Toast만 사용하지 않고 해당 필드의 Inline Validation으로 안내

#### Inline Validation

- 필수값, 날짜 순서, 시간 순서, 숫자 범위를 입력 필드 가까이에 표시
- 사용자가 입력하기 전부터 오류를 과도하게 표시하지 않음
- 제출 시 첫 번째 오류 필드로 포커스 이동
- 여행의 체크아웃은 체크인보다 빠를 수 없고, 도착 일시는 출발 일시보다 빠를 수 없도록 검증
- 주차 위치는 B4~B6, A~D, 1~4 값만 허용

#### Lazy Loading

- 상위 기능별 JavaScript와 무거운 UI는 경로 또는 컴포넌트 단위로 지연 로딩
- 여행 상세의 현재 열지 않은 섹션과 첨부 이미지 등은 필요할 때 로딩
- 사용자에게 즉시 필요한 현재 주차 위치, 현재 월 캘린더, 냉장고 상단 목록은 지연시키지 않음
- 지연 로딩 중에는 크기가 안정적인 대체 UI를 제공해 레이아웃 이동 방지

#### Empty State

각 기능에 단순한 빈 화면 대신 다음 행동이 분명한 안내를 제공한다.

- 캘린더: `이 날짜에는 일정이 없어요` + 일정 추가
- TODO: `아직 할 일이 없어요` + TODO 추가
- 쇼핑목록: `쇼핑목록이 비어 있습니다` + 품목 추가
- 여행: `첫 여행을 기록해 보세요` + 여행 추가
- 냉장고: `등록된 아이템이 없어요` + 아이템 추가
- 주차장: `아직 저장된 주차 위치가 없어요` + 위치 선택 안내
- 랜덤 선택: 후보가 없다는 안내 + 후보 추가

#### 부드러운 Transition

- 일반 UI 전환은 `150~250ms` 범위로 통일
- 버튼과 탭은 약 `150ms`, 모달과 카드 전환은 약 `200~250ms`를 기본값으로 사용
- `opacity`와 `transform` 중심으로 구현해 성능 저하 방지
- 사용자가 운영체제에서 동작 줄이기를 설정한 경우 `prefers-reduced-motion`을 존중해 애니메이션 축소 또는 제거

### 12.2 선택 적용 — 서비스 품질 향상

#### Optimistic UI

다음처럼 성공 가능성이 높고 되돌리기 쉬운 동작에 적용한다.

- TODO 완료 및 미완료 전환
- 쇼핑 품목 구매 완료 및 미완료 전환
- 냉장고 수량 변경 및 소비 완료
- 주차 위치 저장
- 랜덤 후보 활성화 및 비활성화

서버 저장에 실패하면 원래 상태로 되돌리고 Toast로 재시도 방법을 안내한다. 여행 예약 정보처럼 입력량이 많고 데이터 손실 위험이 큰 폼은 서버 성공 전 완료된 것처럼 표시하지 않는다.

#### Auto Save

- 여행 상세의 긴 메모와 여러 입력 항목에는 임시 초안 자동 저장을 검토
- 자동 저장은 입력 직후가 아닌 짧은 debounce 후 수행
- `저장 중`, `저장됨`, `저장 실패` 상태를 화면에 표시
- 생성 폼, 삭제, 주차 위치처럼 사용자의 확정 의도가 중요한 동작에는 명시적 버튼 유지
- 초기 구현 복잡도가 큰 경우 여행 상세 기능 안정화 이후 적용

#### Sticky Header

- 모바일에서 OIP 공통 헤더 또는 현재 화면의 핵심 조작 영역을 상단에 고정
- 캘린더의 연월 이동 영역과 여행 상세의 섹션 탭에 우선 적용
- 작은 화면의 본문을 지나치게 가리지 않도록 높이를 최소화

#### Stagger Animation

- 여행, 냉장고, ETC 후보처럼 카드가 처음 나타나는 짧은 목록에만 미세하게 적용
- 항목당 지연을 작게 하고 전체 애니메이션 시간이 길어지지 않게 제한
- 자주 갱신되는 TODO와 일정 칩에는 과도한 반복 애니메이션을 적용하지 않음
- `prefers-reduced-motion` 설정 시 제거

### 12.3 현재 버전에서 제외하거나 추후 검토

- **Infinite Scroll:** 사용자 2명의 생활 데이터는 한 번에 다룰 양이 많지 않으므로 초기 버전에서는 사용하지 않는다. 데이터가 실제로 많아지면 페이지네이션과 함께 검토한다.
- **Drag & Drop:** 터치 접근성과 저장 순서 처리 비용이 있어 초기 버전에서는 제외한다. TODO 수동 정렬 또는 여행 일정표가 추가될 때 검토한다.
- **Pull To Refresh:** 모바일 브라우저의 기본 새로고침 및 자동 데이터 재검증을 우선 사용한다. PWA로 발전할 경우 다시 검토한다.
- **Shared Element Transition:** 여행 카드에서 상세 화면으로 이동하는 장면 등에 적용 가능하지만 핵심 기능 완성 후 검토한다.
- **Delight Animation:** ETC 랜덤 선택 결과와 TODO 전체 완료처럼 의미 있는 순간에만 짧게 적용한다. 핵심 기능 구현 후 추가한다.
- **Command Palette:** 두 명이 모바일로 사용하는 앱의 규모와 맞지 않아 현재는 구현하지 않는다.
- **Haptic Feedback:** 웹 브라우저와 기기별 지원 차이가 있어 필수 기능으로 사용하지 않는다. 향후 PWA에서 주차 저장 또는 랜덤 선택 결과에 선택적으로 적용한다.
- **Ripple Effect:** 기본 터치·클릭 피드백과 역할이 겹치므로 별도로 구현하지 않는다.

### 12.4 접근성 및 성능 기준

- 모든 애니메이션 없이도 기능과 상태를 이해할 수 있어야 함
- 색상만으로 선택, 작성자, 오류, 완료 상태를 구분하지 않음
- 키보드만으로 주요 기능 사용 가능
- 아이콘 단독 버튼에 접근 가능한 이름 제공
- 저장 또는 오류 Toast는 보조 기술에도 전달
- 화면 진입에 꼭 필요한 데이터와 코드를 우선 로딩
- 이미지가 추가될 경우 크기를 명시하고 Next.js 이미지 최적화 사용

---

## 13. 구현 우선순위

### 13.1 1단계 — 프로젝트 기반

- Next.js 프로젝트 생성
- TypeScript 및 코드 품질 도구 설정
- Vercel 프로젝트 연결
- Supabase 프로젝트 연결
- 공통 비밀번호 검증, 인증 쿠키, 기기 인증 해제 구현
- 공통 레이아웃과 내비게이션 구현
- 모바일 반응형 기준 설정
- 사용자 선택 및 기기 저장 구현
- 공통 버튼, 입력창, 카드, 모달의 hover·focus·touch·transition 상태 구현
- Toast, Inline Validation, Skeleton, Empty State 공통 컴포넌트 구현

### 13.2 2단계 — 핵심 생활 기능

- 주차장 현재 위치 조회 및 업데이트
- 냉장고 아이템 CRUD
- 유통기한 기준 정렬
- 캘린더 기본 월간 화면
- 일정 CRUD
- 대호·상희·공동 일정 기본색과 사용자 지정색 구현
- 공동 및 개인 일정 구분
- 연차·반차·패밀리데이·해피프라이데이 휴무 추가 및 배경 표시
- 두 사용자 휴무 중복 시 2색 그라데이션 표시
- 기념일 기본 데이터
- 한국천문연구원 공휴일 동기화 및 캐시
- TODO CRUD 및 완료 처리
- 쇼핑 품목 CRUD 및 구매 완료 처리
- 쇼핑목록 공동 실시간 동기화

### 13.3 3단계 — 여행

- 여행 목록 및 여행 CRUD
- 출발일 기준 예정 여행 정렬 및 지난 여행 분리
- 여행 상세 화면
- 비행기 정보
- 숙소 정보
- 교통 정보
- 먹고 싶은 것
- 가고 싶은 곳

### 13.4 4단계 — ETC

- 랜덤 여행지 후보 관리 및 선택
- 랜덤 식사 메뉴 후보 관리 및 선택

### 13.5 5단계 — 안정화 및 개선

- 대한민국 공휴일 데이터 적용
- Supabase RLS 및 인증 정책 확정
- 로딩 및 오류 UX 개선
- 접근성 점검
- 모바일 실기기 테스트
- E2E 핵심 시나리오 테스트
- 필요 시 Supabase Realtime 적용
- Optimistic UI 실패 복구 시나리오 점검
- 여행 상세 Auto Save 적용 여부 확정
- 필요한 화면에만 Stagger 및 Delight Animation 적용

---

## 14. 테스트 기준

### 사용자

- 인증되지 않은 기기에서는 공통 비밀번호 화면이 먼저 표시된다.
- 잘못된 비밀번호로 생활 데이터에 접근할 수 없다.
- 올바른 비밀번호 입력 후 인증 상태가 기기에 유지된다.
- 기기 인증 해제 후 다시 비밀번호 입력 화면이 표시된다.
- 새 기기에서 대호 또는 상희를 선택할 수 있다.
- 새로고침 후에도 선택한 사용자가 유지된다.
- 헤더에서 현재 사용자를 확인하고 변경할 수 있다.

### 캘린더, TODO 및 쇼핑목록

- 공동 항목은 두 사용자 모두 볼 수 있다.
- 개인 항목은 작성자 화면에서만 보인다.
- 작성자를 구분할 수 있다.
- 일정과 TODO를 추가, 수정, 삭제할 수 있다.
- 기본 기념일이 매년 올바른 날짜에 표시된다.
- 대호, 상희, 공동 일정이 각 기본색으로 표시된다.
- 사용자가 일정 색상을 직접 변경할 수 있다.
- 공동 일정 체크 시 공동 기본색이 적용된다.
- 연차와 기타 휴무가 일정 칩이 아닌 연한 날짜 배경으로 표시된다.
- 대호와 상희의 휴무가 같은 날이면 두 색이 함께 표시된다.
- 대호는 패밀리데이, 상희는 해피프라이데이를 등록할 수 있다.
- 공식 공휴일 동기화 실패 시에도 마지막 저장 데이터가 표시된다.
- 쇼핑 품목을 이름만으로 빠르게 추가할 수 있다.
- 대호와 상희가 같은 쇼핑목록을 확인할 수 있다.
- 품목을 구매 완료 또는 미완료로 전환할 수 있다.
- 구매 완료한 사용자와 시간이 기록된다.
- 구매 상태 저장 실패 시 이전 상태로 정상 복구된다.

### 여행

- 여행을 추가, 수정, 삭제할 수 있다.
- 예정 및 진행 중인 여행이 출발일이 가까운 순서로 표시된다.
- 종료일이 지난 여행은 별도의 지난 여행 영역에 표시된다.
- 한 여행에 비행기, 숙소, 교통, 음식, 장소 정보를 여러 개 등록할 수 있다.

### 냉장고

- 아이템을 추가, 수정, 삭제 또는 소비 완료 처리할 수 있다.
- 유통기한 없이는 아이템을 등록할 수 없다.
- 유통기한이 임박한 순서로 정확히 정렬된다.
- 유통기한이 지난 아이템이 명확히 표시된다.

### 주차장

- 현재 주차 위치가 화면 최상단에 표시된다.
- B4~B6 및 유효한 기둥 위치만 입력할 수 있다.
- 저장한 사람과 저장 시간이 표시된다.
- 두 기기에서 최신 주차 위치를 확인할 수 있다.

### ETC

- 활성화된 후보만 랜덤 선택에 포함된다.
- 후보가 없을 때 적절한 안내가 표시된다.
- 후보가 한 개일 때도 오류 없이 결과를 표시한다.

### 공통 UX

- 모든 버튼과 링크에 touch, hover, focus 피드백이 있다.
- 모바일에서 버튼을 누르는 즉시 pressed 상태가 표시된다.
- 주요 버튼과 자주 누르는 체크 영역이 최소 터치 크기를 충족한다.
- 스크롤 동작 중 버튼이나 목록 항목이 의도치 않게 실행되지 않는다.
- 빠르게 여러 번 눌러도 저장 동작이 중복 실행되지 않는다.
- 데이터 조회 중 레이아웃에 맞는 Skeleton이 표시된다.
- 빈 데이터 상태에서 사용자가 다음 행동을 알 수 있다.
- 폼 오류가 해당 입력 필드 가까이에 표시된다.
- 저장 결과가 Toast 또는 화면 내부 상태로 전달된다.
- Optimistic UI 저장 실패 시 이전 상태로 정상 복구된다.
- 일반 전환이 150~250ms 범위이며 동작 줄이기 설정을 존중한다.
- 키보드만으로도 주요 기능을 사용할 수 있다.

---

## 15. 추가 확인 및 결정이 필요한 사항

아래 내용은 추가 의견을 받은 뒤 확정한다.

1. 공동 항목 편집 권한  
   상대방이 작성한 공동 일정과 TODO도 수정 및 삭제할 수 있는지 결정이 필요하다.
2. 캘린더 최종 색상값  
   대호, 상희, 공동, 휴무와 시스템 일정의 실제 색상값은 UI 시안 작업 시 확정한다.

---

## 16. 참고 이미지

다음 이미지를 UI 설계 참고 자료로 사용한다.

- [`calander_example.jpeg`](./calander_example.jpeg): 월간 캘린더의 날짜 그리드, 일정 색상 칩, 여러 날에 걸친 일정 표시 참고
- [`parking_example.jpeg`](./parking_example.jpeg): 현재 위치 카드, 수정자·수정 시각, 층·기둥 선택 그리드, 동적 저장 버튼 참고

참고 이미지는 정보 구조와 사용 흐름의 기준이며 그대로 복제하지 않는다. OIP의 공통 헤더, 사용자 구분, 전체 탭, 접근성, 반응형 UI와 일관되도록 재구성한다.
